# 剩余功能 TODO（2026-04-17 收工版）

## 当前状态
- 已完成三阶段平台能力建设（底座、传统模块、权限与导入导出）。
- 部门层级数据权限核心代码已收口；可见性规则已完成运行态实测。

## 剩余功能清单
- [x] 后端：完成部门层级结构收口（`Department.parentId` 全链路校验、更新、删除约束）。
- [x] 后端：将用户 `dataScope=dept` 范围稳定为“本部门 + 全部子部门”，并处理异常路径。
- [x] 前端：部门管理页补充父部门字段（创建/编辑/列表展示）。
- [x] 前端：用户页数据范围文案从“本部门数据”升级为“本部门及子部门数据”。
- [x] 联调：构造父子部门样本，验证可见性（本部门+子部门可见、无关部门不可见）。
- [x] 文档：更新 `README.md` 与会话计划，标记该能力完成。

## 下次继续建议起点
1. 按业务场景继续补充演示数据，便于前端模块走查。
2. 开始新模块需求时复用当前联调数据作为基线样本。

## 联调准备（不运行）
1. 部门样本：创建 A 总部（parentId=0）、B 财务（parentId=A）、C 出纳（parentId=B）、D 人事（parentId=A）、E 外部（parentId=0）。
2. 角色样本：准备一个 `dataScope=dept` 的角色（如 operator）。
3. 用户样本：U1 绑定角色 operator 且 deptId=B；U2 deptId=C；U3 deptId=D；U4 deptId=E。
4. 期望结果：U1 登录后，用户列表可见 U1/U2，不可见 U3/U4；导出 CSV 结果与列表范围一致。
5. 异常路径：尝试将部门 B 的父部门改为 C，预期被拒绝；尝试删除有子部门或有用户的部门，预期被拒绝。

## 联调进度（2026-04-17）
- [x] 可见性验证（U1 仅可见 U1/U2）：通过。
- [x] 导出范围验证（CSV 与列表一致）：通过。
- [x] 父部门循环校验（B 改父为 C 被拒绝）：通过。
- [x] 删除约束校验（有子部门/有用户不可删）：通过。

## 可见性代码路径验收结论
1. `scopedUserQuery` 在 `dataScope=dept` 时通过 `collectDepartmentSubtreeIDs(current.DeptID)` 获取“本部门+全部子部门”。
2. 查询条件为 `WHERE dept_id IN (?)`，天然排除无关部门用户。
3. 仅当用户无部门或异常场景时回落为仅本人，避免越权扩大范围。
