# 平台剩余工作 TODO（2026-04-20）

## 当前基线
- 前后端已稳定联调（Web: 2341 / API: 2342）。
- 安全闭环已完成（验证码、锁定、续签、会话强退、主动登出）。
- 消息中心已完成实时未读链路（SSE + stream token）与“全部已读”。
- 文件中心已完成筛选、批量上传、进度反馈与任务结果明细。
- 个人中心已支持显示名/密码/邮箱/手机号/头像地址维护。
- 测试基线已具备（构建、API 契约、分层测试脚手架）。

## 剩余优先级 TODO

### P0（建议优先）
- [ ] CI 门禁：新增 GitHub Actions，至少覆盖 `build:web`、`build:api`、`test:api-contract`、`test:security-smoke`。
- [ ] 测试补强：为 `messages/stream-token`、`messages/read-all`、个人中心新增字段、文件批量上传关键路径补充自动化回归。
- [ ] 发布稳定性：统一本地/CI 启动脚本与环境变量模板，避免因旧进程或配置漂移导致联调误报。

### P1（体验与性能）
- [ ] 包体二阶段优化：继续压缩 `vendor-tdesign`（目标：进一步降到可控范围），评估按页面拆分重组件与懒加载策略。
- [ ] 消息体验：支持“未读来源定位/跳转”与消息列表的批量操作反馈优化。
- [ ] 文件体验：补上传失败重试、断点续传方案评估、下载失败可观测提示。

### P2（平台能力）
- [ ] 文件存储抽象：从本地 `uploads/` 升级为可切换存储驱动（本地/MinIO/对象存储）。
- [ ] 消息通道扩展：在 SSE 基础上评估 WebSocket 与异步推送通道（站内信模板、订阅策略）。
- [ ] 个人中心增强：支持头像文件上传（替代 URL 手填）与资料完整度策略化校验。

## 验收口径（DoD）
- [ ] 所有新增能力具备可重复执行的回归命令并写入 README。
- [ ] 关键链路在无手工干预下可通过 CI。
- [ ] 功能变更均有对应权限与错误提示，不出现静默失败。
