# Cursor Prompt Template

Use project command flow:

```bash
npm run module:plan -- --spec specs/modules/<module>.module.spec.json
npm run module:apply -- --spec specs/modules/<module>.module.spec.json
```

